{
  # "development" will produce detailed error message pages
  # "production" will render basic error pages
  runtime_mode => "development",

  # persistent storage for list of hosts
  persist_dir  => "/var/lib/tailscale/mojo",
}
